/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://flickday.to', // Change this to your actual domain
    generateRobotsTxt: true, // Automatically creates robots.txt
    sitemapSize: 5000, // Maximum URLs per sitemap file
  };
  