export type WatchPageProps = {
    params: {
      mediaType: string,
      id: string,
      title?: string
    }
  }
  export type ReadMangaPageProps = {
    params: {
      id: string,
      title?: string
    }
  }